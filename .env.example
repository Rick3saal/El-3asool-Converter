# All settings are optional — the app needs no secrets to run.
# Values are read at BUILD time in vite.config.ts (never embedded in the client bundle).

# Absolute origin used for the canonical link and the Open Graph share image.
# Leave empty to use Vercel's deployment host (VERCEL_PROJECT_PRODUCTION_URL / VERCEL_URL).
SITE_URL=
